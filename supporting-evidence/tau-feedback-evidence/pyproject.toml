[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "tau-feedback-evidence"
version = "0.1.0"
description = "Reproducible evidence-contract experiments for tau agent evaluation"
requires-python = ">=3.12"
dependencies = []

[tool.setuptools.packages.find]
where = ["src"]
